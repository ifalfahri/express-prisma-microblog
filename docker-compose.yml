services:
  postgres:
    image: postgres:10.3
    restart: always
    environment:
      - POSTGRES_USER=microblog
      - POSTGRES_PASSWORD=inipassword
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - '5432:5432'
volumes:
  postgres: